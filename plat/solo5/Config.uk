menuconfig PLAT_SOLO5
       bool "SOLO5 platform"
       default n
       depends on (ARCH_X86_64)
       select LIBUKDEBUG
       select LIBNOLIBC if !HAVE_LIBC
       help
                Create a Unikraft image that runs in solo5 environment

#if (PLAT_SOLO5)
#endif
